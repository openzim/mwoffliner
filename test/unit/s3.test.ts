import test from 'blue-tape';
import Axios from 'axios';
import S3 from '../../src/util/s3';

test('s3 checks', async(t)=>{
    // const imageUpload = await S3.uploadImage(axiosResp, imageUrl);
    // t.ok(imageUpload, 'Image is successfully uploaded');
})