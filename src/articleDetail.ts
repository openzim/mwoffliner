
export const articleDetailXId: {
    [articleId: string]: {
        subCategories?: PageInfo[],
        categories?: PageInfo[],
        thumbnail?: string,
        title: string,
        oldId: number,
    },
} = {};
